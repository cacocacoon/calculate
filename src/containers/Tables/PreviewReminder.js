import {connect} from 'react-redux';
import PreviewReminderTable from '../../components/Tables/PreviewReminderTable';

import {
} from '../../actions';

export default connect(
	(state) => ({

	}),

	(dispatch) => ({
	}),
)(EditorModal);
